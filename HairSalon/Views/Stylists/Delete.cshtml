@model HairSalon.Models.Stylist;

@{
  Layout = "_Layout";
}

<div class="container">
  <div class="card">
    <h3 class="p-2 border-bottom">Delete Stylist - @Model.Name</h3>
    <div class="container">
      @using (Html.BeginForm())
      {
        <input type="submit" class="btn btn-lg btn-block btn-danger mt-2 mb-2" value="Permanently Delete Stylist" />
      }
    </div>
  </div>
</div>
