@using HairSalon.Models;

@{
  Layout = "_Layout";
}

<div class="container">
  <div class="card">
    <h3 class="p-2 border-bottom">Viewing All Clients</h3>

    @if (@Model.Count == 0)
    {
      <h4 class="p-5">No clients have been added yet</h4>
    }
    else
    {
      <div class="container p-2">
        <ol>
          @foreach (Client client in Model)
          {
            <li>@Html.ActionLink($"{client.Name}", "Details", new { id = client.ClientId })</li>
          }
        </ol>
      </div>
    }

  </div>
  <p>@Html.ActionLink("Add a new Client", "Create", "Clients", null, new { @class = "btn btn-info btn-block btn-lg" })</p>
</div>
